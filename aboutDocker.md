Why DOCKER-
1- Environmnet Reproducibility: Everyone gets the exact same setup.
2- Dependency Management.
3- Portability: We can run the container anywhere on any machine.
4- Version Control: HOW?


SERVER-
    DEFINITION?
    Scaling Servers: We have to scale the server with the increasing number of the users.
    -Multiple applications can be installed and run in the same server. Each requiring their own runtime(software the executes the code) versions, dependencies, etc.

-To resolve various issues that might arise due to all this, we use the concept called "Virtualization"-> The technology that allows one physical computer to behave as multiple independent computers(virtual machines). These VMs are managed by Hypervisor{a software that creates and manages the VMs).

HOW AND WHY CONTAINERS ARE FASTER THAN VMs?


